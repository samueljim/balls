# Point the Next.js app at your Cloudflare Worker (HTTP + WebSockets).
# For worms.bne.sh: use https://api.worms.bne.sh (see README).
# Otherwise: https://worms-worker.<your-subdomain>.workers.dev after pnpm deploy:worker
NEXT_PUBLIC_API_BASE=https://api.worms.bne.sh

# Optional: WebSocket base. Defaults to NEXT_PUBLIC_API_BASE with http -> ws.
# NEXT_PUBLIC_WS_BASE=wss://api.worms.bne.sh


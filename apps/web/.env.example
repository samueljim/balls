# Point the Next.js app at your Cloudflare Worker (HTTP + WebSockets).
# For balls.bne.sh: use https://api.balls.bne.sh (see README).
# Otherwise: https://balls-worker.<your-subdomain>.workers.dev after pnpm deploy:worker
NEXT_PUBLIC_API_BASE=https://api.balls.bne.sh

# Optional: WebSocket base. Defaults to NEXT_PUBLIC_API_BASE with http -> ws.
# NEXT_PUBLIC_WS_BASE=wss://api.balls.bne.sh


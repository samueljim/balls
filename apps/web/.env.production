# Production environment variables for the web app.
# This file is used by `next build` when NODE_ENV=production.
# In CI you can either commit this file (no secrets here) or set the
# equivalent environment variables in your CI/hosting provider.

# Backend API (no port) — production backend host
NEXT_PUBLIC_API_BASE=https://balls.bne.sh

# WebSocket endpoint for production (wss for secure websockets)
NEXT_PUBLIC_WS_BASE=wss://api.balls.bne.sh
F
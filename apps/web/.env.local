# Local development API / WebSocket endpoints. This file is not committed to
# production — set the corresponding env vars in your production host instead.
NEXT_PUBLIC_API_BASE=http://localhost:8787
NEXT_PUBLIC_WS_BASE=ws://localhost:8787